{
  "name": "zack-tarbet-webinars",
  "version": "1.0.0",
  "private": true,
  "description": "Static webinar registration site for Zack Tarbet (OriginPoint).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "check": "node build.mjs --check"
  },
  "engines": {
    "node": ">=18"
  }
}
